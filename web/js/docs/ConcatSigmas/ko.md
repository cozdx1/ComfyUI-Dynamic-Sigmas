# 시그마 결합

연결된 시그마 스케줄을 입력 번호 순서대로 결합합니다.

**입력 개수**를 늘리면 입력이 추가됩니다. 비어 있거나 연결되지 않은 입력은 무시합니다. 공통 경계값이 중복되지 않도록 마지막 스케줄을 제외한 각 스케줄의 끝값을 제거합니다.

예를 들어 `[1.0, 0.5]` 뒤에 `[0.5, 0.0]`을 연결하면 `[1.0, 0.5, 0.0]`이 됩니다.

서로 이웃한 경계값을 자동으로 일치시키지는 않습니다. 연속된 곡선이 필요하다면 앞 스케줄의 끝값과 다음 스케줄의 시작값을 확인하세요.
